architecture: standalone

auth:
  enabled: false

master:
  persistence:
    enabled: true
    storageClass: ebs-gp3
    size: 4Gi

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 250m
      memory: 256Mi

service:
  type: ClusterIP