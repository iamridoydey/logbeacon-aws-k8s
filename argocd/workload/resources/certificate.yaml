apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: cert-manager-workload-resources
  namespace: argocd

  annotations:
    argocd.argoproj.io/sync-wave: "25"

spec:
  project: default

  source:
    repoURL: https://github.com/iamridoydey/logbeacon-aws-k8s.git
    targetRevision: main
    path: infra-resources/cert-manager/workload

  destination:
    name: workload-cluster
    namespace: logbeacon

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true