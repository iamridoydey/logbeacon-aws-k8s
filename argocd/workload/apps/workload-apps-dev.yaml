apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: logbeacon-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/iamridoydey/logbeacon-aws-k8s.git
    targetRevision: dev
    path: apps/logbeacon/overlays/dev

  destination:
    name: workload-cluster
    namespace: logbeacon

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
